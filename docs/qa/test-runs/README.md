# QA Test Runs

Curated test-run evidence belongs here when logs or artifacts are useful for future review.

Keep raw local run output in scratch paths such as `apps/ios/artifacts/` unless it is intentionally promoted.
